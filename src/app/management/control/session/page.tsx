import Session from "@/components/control/session/Session";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return (
    <div>
      <Session />
    </div>
  );
};

export default Page;
