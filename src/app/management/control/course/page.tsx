import Course from "@/components/control/course/Course";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Course />
    </div>
  );
};

export default page;
