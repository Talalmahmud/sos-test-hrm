import React from "react";
import TargetControl from "../../../../components/control/target/TargetControl";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <TargetControl />
    </div>
  );
};

export default page;
