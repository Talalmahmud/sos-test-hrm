import MyCourse from "@/components/course/my/MyCourse";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <MyCourse />
    </div>
  );
};

export default page;
