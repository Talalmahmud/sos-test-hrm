import TodayCourse from "@/components/course/today/TodayCourse";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <TodayCourse />
    </div>
  );
};

export default page;
