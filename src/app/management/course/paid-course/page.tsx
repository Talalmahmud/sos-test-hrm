import Paid from "@/components/course/paid/Paid";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Paid />
    </div>
  );
};

export default page;
