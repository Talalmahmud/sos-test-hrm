import Pending from "@/components/course/pending/Pending";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Pending />
    </div>
  );
};

export default page;
