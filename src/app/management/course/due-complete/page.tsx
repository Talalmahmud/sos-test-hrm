import DueComplete from "@/components/course/due/DueComplete";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <DueComplete />
    </>
  );
};

export default page;
