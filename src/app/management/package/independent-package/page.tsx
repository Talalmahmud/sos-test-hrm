import IndependentPackage from "@/components/package/independent/IndependentPackage";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <IndependentPackage />
    </div>
  );
};

export default page;
