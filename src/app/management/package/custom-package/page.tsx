import CustomPackage from "@/components/package/custom/CustomPackage";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <CustomPackage />
    </div>
  );
};

export default page;
