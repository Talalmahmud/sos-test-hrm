import PackagePlanPrice from "@/components/package/PackagePlanPrice";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <PackagePlanPrice />
    </div>
  );
};

export default page;
