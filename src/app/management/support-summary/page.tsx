import SupportSummary from "@/components/supports/SupportSummary";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <SupportSummary />
    </div>
  );
};

export default page;
