import AddBook from "@/components/book/add/AddBook";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <AddBook />
    </div>
  );
};

export default page;
