import BookPage from "@/components/book/BookPage";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return (
    <div>
      <BookPage />
    </div>
  );
};

export default Page;
