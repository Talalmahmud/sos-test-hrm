import BookProcessing from "@/components/book/processing/BookProcessing";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <BookProcessing />
    </div>
  );
};

export default page;
