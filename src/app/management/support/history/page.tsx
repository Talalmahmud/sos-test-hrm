import SupportHistory from "@/components/supports/SupportHistory";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div className=" w-full">
      <SupportHistory />
    </div>
  );
};

export default page;
