import Support from "@/components/supports/Support";
import SupportTable from "@/components/supports/SupportTable";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Support status="created" />
    </div>
  );
};

export default page;
