import AddPermission from "@/components/role-permission/AddPermission";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return (
    <div>
      <AddPermission />
    </div>
  );
};

export default Page;
